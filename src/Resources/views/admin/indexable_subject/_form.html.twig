{# @var resource \Setono\SyliusMeilisearchPlugin\Model\IndexableSubject #}
{# @var help string a translation key for the help text in the sidebar #}
{# @var subject_field string the name of the form field binding the row to its product attribute/option #}
{{ form_errors(form) }}
<div class="ui two column stackable grid">
    <div class="ten wide column">
        {{ form_row(form[subject_field]) }}

        <div class="ui three fields">
            {{ form_row(form.searchable) }}
            {{ form_row(form.filterable) }}
            {{ form_row(form.facetable) }}
        </div>

        <div class="ui three fields">
            {{ form_row(form.facetPosition) }}
            {{ form_row(form.indexes) }}
            {{ form_row(form.enabled) }}
        </div>
    </div>
    <div class="six wide column">{{ help|trans|raw }}</div>
</div>
